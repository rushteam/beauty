global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: otel-collector
    # 保留 Collector 按 service.name 生成的 job/instance 标签，而不是覆盖成 otel-collector
    honor_labels: true
    static_configs:
      - targets: ["otel-collector:8889"]
